import { PickFirstLettersPipe } from './pick-first-letters.pipe'

describe('PickFirstLettersPipe', () => {
	it('create an instance', () => {
		const pipe = new PickFirstLettersPipe()
		expect(pipe).toBeTruthy()
	})
})
